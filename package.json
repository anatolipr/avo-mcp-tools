{
  "name": "avo-mcp-tools-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p packages/mcp-tenant-server/tsconfig.json && npm run typecheck -w mcp-form"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
